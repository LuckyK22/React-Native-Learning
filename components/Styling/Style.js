import { StyleSheet } from 'react-native';

const style = StyleSheet.create({

    container: {
        margin: 10,
    },
    heading: {
        fontSize: 24,
    }
}); 

export { style }